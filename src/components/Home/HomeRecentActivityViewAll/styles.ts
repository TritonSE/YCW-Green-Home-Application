import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  title: {
    fontSize: 16,
    paddingLeft: '2%',
    paddingTop: '3%',
    fontWeight: '500',
  },
});

export default styles;
