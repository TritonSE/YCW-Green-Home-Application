import { StyleSheet } from 'react-native';

// For now, taken entirely from Shravan and Upo's work on their Task page.
// Thanks Shravan and Upo!

const styles = StyleSheet.create({
  container: {
    flexGrow: 1,
    backgroundColor: 'rgb(250,250,250)',
  },
  header: {
    zIndex: 10,
    elevation: 10,
  },
});

export default styles;
